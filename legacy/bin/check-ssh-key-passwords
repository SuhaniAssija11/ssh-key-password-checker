#!/usr/bin/env bash
# Wrapper for the SSH key passphrase checker.
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "${SCRIPT_DIR}/../scripts/check_ssh_key_passwords.py" "$@"
